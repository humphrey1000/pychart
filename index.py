import sys


def parser(prose):
    """ \
            Parsing data
        Data types: variables, functions, data structures, operators, classes
    """

    if __name__ == '__main__':
        prose = sys.argv[1]
        parser(prose)
